{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "dev:admin-core": "cargo xtask dev --admin-core",
    "dev:admin-tauri": "cargo xtask dev --admin-tauri",
    "dev:client-core": "cargo xtask dev --client-core",
    "dev:client-tauri": "cargo xtask dev --client-tauri",
    "dev:go-server": "cargo xtask dev --go-server --move",
    "build:admin-core": "cargo xtask build --admin-core",
    "build:admin-tauri": "cargo xtask build --admin-tauri",
    "build:client-core": "cargo xtask build --client-core",
    "build:client-tauri": "cargo xtask build --client-tauri",
    "build:go-server": "cargo xtask build --go-server --move"
  }
}
